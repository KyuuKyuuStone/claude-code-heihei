# Claude Code Heihei v1.0.8

本版本按 DeepSeek 官方定价文档更新协作服务商预置。

## DeepSeek 预置更新

- **主力模型切换为 `deepseek-flash`**（V4.1-Flash：1M 上下文、支持图像理解、官方称全面超越 V4 Pro）。
- 缘由：DeepSeek 官方公告，**9/14 12:00 起 `deepseek-v4-pro` 的请求将全部路由到 V4.1 Flash 并按 Flash 计费**——提前切换避免计费与性能预期混乱。
- 注意 V4.1 Flash 采用**峰谷计价**：高峰时段（工作日 9:00–12:00、14:00–18:00）价格为空闲时段的两倍，重度使用可错峰。

## 升级说明

- 已保存的 DeepSeek 方案会在下次启动时按新预置解析主力模型；也可以在方案里手动改回 `deepseek-v4-pro`（9/14 后仍可调用，只是被路由并按 Flash 计费）。
